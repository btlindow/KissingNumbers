# build trees and run artefacts
build/
runs/

# large generated data (regenerated by gen_leech / build_adj; see docs/design.md §2.2)
data/adj.u32
data/*.i8
data/*.u32

# external clones (T1.3 copies the small fixtures out)
data/external/

# python
.venv/
venv/
__pycache__/
*.pyc
.pytest_cache/

# editors / misc
.cache/
*.swp
.DS_Store

# LaTeX build artefacts (the PDF itself is committed)
docs/note/*.aux
docs/note/*.log
docs/note/*.out
docs/note/*.toc

# Markdowns
AGENTS.md
# Phase 2 working directory (local only)
PHASE_2/

# Confidential to the collaboration (Cohn construction) — never public
private/
# material received from others — never public
inbox/
outbox/

# regenerable caches
.cache/

# solver scratch
*.lp
*.mps
*.sol
